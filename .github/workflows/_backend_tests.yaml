name: Backend tests

on:
  workflow_call:

jobs:
  backend-tests:
    name: Backend tests
    timeout-minutes: 60
    runs-on: "ubuntu-latest"
    defaults:
      run:
        shell: bash
    services:
      postgres:
        image: postgres:18.4-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: personal_workspace_database_test
        options: >-
          --health-cmd "pg_isready -U postgres -d personal_workspace_database_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 55432:5432
    steps:
      - uses: actions/checkout@v7
      - name: "Set up Python"
        uses: actions/setup-python@v7
        with:
          python-version-file: "pyproject.toml"
      - name: "Install UV"
        uses: astral-sh/setup-uv@v10.0.1
        with:
          cache-suffix: "backend-tests"
      - name: "Run backend tests with coverage"
        run: make tests-coverage
      - name: "Coverage Badge"
        run: |
          pip install -U genbadge defusedxml
          genbadge coverage -n backend -i coverage.xml -o .github/badges/coverage-backend.svg
      - name: "Upload coverage badge"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: personal-workspace-coverage-badge-${{ github.sha }}
          path: .github/badges/coverage-backend.svg
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 1
